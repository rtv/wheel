#
# An example of how to build a Stage controller plugin with libtool
#

CXX = g++
CXXLD = g++
LIBTOOL = libtool
CFLAGS += `pkg-config --cflags stage`

all: wheel.so

wheel.so: wheel.cc
	$(CXX) $(CFLAGS) -bundle -c $<

clean:
	rm -rf *.o *.lo *.la .libs *.so
